# Быстрый старт - Открытие браузера

## Проблема: Двойной клик по ссылке в терминале не открывает браузер

Это нормальное поведение PowerShell - терминалы не открывают ссылки автоматически при клике.

## ✅ Решения:

### Вариант 1: Использовать готовые скрипты (рекомендуется)

**Для запуска всего проекта:**
```powershell
.\start-dev.ps1
```

**Для запуска только Backend:**
```powershell
cd backend
.\runserver.ps1
```

Или:
```powershell
cd backend
python runserver.py
```

Эти скрипты автоматически откроют браузер через 2 секунды после запуска сервера.

### Вариант 2: Открыть браузер вручную командой

**В PowerShell:**
```powershell
Start-Process "http://127.0.0.1:8000/"
```

**Или используйте скрипт:**
```powershell
.\open-browser.ps1
```

### Вариант 3: Скопировать ссылку и вставить в браузер

1. Выделите ссылку `http://127.0.0.1:8000/` в терминале
2. Нажмите `Ctrl+C` для копирования
3. Откройте браузер и вставьте в адресную строку (`Ctrl+V`)

### Вариант 4: Настроить PowerShell для открытия ссылок

Можно добавить функцию в профиль PowerShell:

```powershell
# Откройте профиль PowerShell
notepad $PROFILE

# Добавьте эту функцию:
function Open-Url {
    param([string]$Url)
    Start-Process $Url
}

# Сохраните и перезапустите PowerShell
# Теперь можно использовать: Open-Url "http://127.0.0.1:8000/"
```

## 🔧 Если скрипты не работают:

1. **Проверьте политику выполнения:**
   ```powershell
   Get-ExecutionPolicy
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

2. **Убедитесь, что сервер запущен:**
   ```powershell
   cd backend
   python manage.py runserver
   ```

3. **Проверьте, что порт 8000 свободен:**
   ```powershell
   netstat -ano | findstr :8000
   ```

## 💡 Совет

Лучше всего использовать скрипт `start-dev.ps1` или `backend/runserver.ps1` - они автоматически откроют браузер после запуска сервера.

