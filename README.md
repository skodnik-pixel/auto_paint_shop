# Auto Paint Shop 🚗

Веб-приложение для интернет-магазина автокосметики и материалов для кузовного ремонта.

## 🚀 Технологии

### Backend
- **Django 4.x** — веб-фреймворк
- **Django REST Framework** — API
- **SQLite** — база данных (для разработки)
- **Python 3.8+**

### Frontend
- **React 18** — пользовательский интерфейс
- **React Bootstrap** — UI компоненты
- **React Router** — навигация
- **Axios** — HTTP клиент

## 📁 Структура проекта

```
auto_paint_shop/
├── backend/                 # Django приложение
│   ├── accounts/            # Управление пользователями
│   ├── catalog/             # Каталог товаров, категории, бренды
│   ├── cart/                # Корзина покупок
│   ├── core/                # Настройки сайта, страницы, отчёты
│   ├── orders/              # Заказы
│   └── shop/                # Настройки Django, URL, админка
├── frontend/                # React приложение
│   ├── public/              # Статические файлы
│   └── src/
│       ├── components/      # Компоненты (каталог, корзина, страницы)
│       ├── context/         # React Context (авторизация)
│       ├── services/        # API-сервисы
│       └── utils/           # Утилиты
├── .gitignore
└── README.md
```

## 🛠️ Установка и запуск

### Backend (Django)

1. **Перейдите в папку backend и создайте виртуальное окружение:**
```bash
cd backend
python -m venv venv
```

2. **Активируйте виртуальное окружение:**
```bash
# Windows
venv\Scripts\activate

# Linux / macOS
source venv/bin/activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Выполните миграции:**
```bash
python manage.py migrate
```

5. **Создайте суперпользователя (для входа в админку):**
```bash
python manage.py createsuperuser
```

6. **Запустите сервер разработки:**
```bash
python manage.py runserver
```

Backend будет доступен по адресу: **http://127.0.0.1:8000/**

### Frontend (React)

В **другом** терминале:

1. **Перейдите в папку frontend и установите зависимости:**
```bash
cd frontend
npm install
```

2. **Запустите сервер разработки:**
```bash
npm start
```

Frontend откроется в браузере: **http://localhost:3000**

### Запуск в разработке (два терминала)

- **Терминал 1:** `cd backend` → активировать venv → `python manage.py runserver`
- **Терминал 2:** `cd frontend` → `npm start`

## 🌐 Доступ к приложению

| Сервис        | URL                      |
|---------------|--------------------------|
| Frontend      | http://localhost:3000    |
| Backend API   | http://127.0.0.1:8000/api |
| Админ-панель  | http://127.0.0.1:8000/admin |

## 📋 Функциональность

### Для пользователей
- Просмотр каталога товаров
- Поиск и фильтрация по категориям и брендам
- Добавление товаров в корзину
- Оформление заказов
- Регистрация и авторизация (JWT)
- Просмотр истории заказов и профиль

### Для администраторов
- Управление товарами, категориями, брендами
- Управление заказами и пользователями
- Настройки сайта, страницы, отчёты (модуль core)

## 🗂️ Категории товаров

- **Автокосметика** — шампуни, воски, полироли
- **Автохимия** — очистители, обезжириватели
- **Инструменты** — щётки, губки, полотенца
- **Защитные покрытия** — керамика, воски, герметики

## 🔧 API Endpoints

### Каталог
- `GET /api/catalog/products/` — список товаров (параметры: `search`, `category`, `brand`)
- `GET /api/catalog/products/{slug}/` — детали товара по slug
- `GET /api/catalog/categories/` — список категорий
- `GET /api/catalog/brands/` — список брендов

### Корзина
- `GET /api/cart/` — содержимое корзины
- `POST /api/cart/add_item/` — добавить товар в корзину
- Удаление и обновление — через ViewSet (см. `cart/urls.py`)

### Заказы
- `GET /api/orders/orders/` — список заказов пользователя
- `POST /api/orders/orders/create_order/` — создание заказа

### Аутентификация
- `POST /api/auth/jwt/create/` — получение JWT (логин)
- `POST /api/auth/jwt/refresh/` — обновление токена
- Регистрация и сброс пароля — Djoser: `api/auth/`

## 📝 Переменные окружения

При необходимости создайте файл `.env` в папке **backend** (или в корне проекта, в зависимости от настроек в `settings.py`):

```env
# Django
SECRET_KEY=your-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# React (в frontend при сборке)
REACT_APP_API_URL=http://127.0.0.1:8000/api
```

Файл `.env` обычно добавлен в `.gitignore` и не коммитится.

## 🤝 Вклад в проект

1. Сделайте форк репозитория.
2. Создайте ветку: `git checkout -b feature/amazing-feature`
3. Зафиксируйте изменения: `git commit -m 'Add amazing feature'`
4. Отправьте в ветку: `git push origin feature/amazing-feature`
5. Откройте Pull Request.

## 📄 Лицензия

Проект распространяется под лицензией MIT. Подробности — в файле `LICENSE`.

## 📞 Поддержка

Вопросы и предложения — через Issues в репозитории.
