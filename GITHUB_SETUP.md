# Инструкция по добавлению проекта на GitHub

## 📋 Текущий статус

✅ Git репозиторий уже инициализирован
✅ Подключен к удаленному репозиторию (origin/main)
⚠️ Есть незакоммиченные изменения

## 🚀 Шаги для публикации проекта

### Вариант 1: Если репозиторий уже существует на GitHub

1. **Добавьте все изменения:**
   ```powershell
   git add .
   ```

2. **Создайте коммит:**
   ```powershell
   git commit -m "Обновление проекта: добавлен желто-черный дизайн, улучшена обработка slug, добавлены скрипты для Windows"
   ```

3. **Отправьте изменения на GitHub:**
   ```powershell
   git push origin main
   ```

### Вариант 2: Если нужно создать новый репозиторий на GitHub

1. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com
   - Нажмите "+" → "New repository"
   - Название: `auto_paint_shop` (или другое)
   - Описание: "Интернет-магазин автокосметики на Django REST Framework + React"
   - Выберите Public или Private
   - **НЕ** создавайте README, .gitignore или лицензию (они уже есть)

2. **Добавьте все изменения:**
   ```powershell
   git add .
   ```

3. **Создайте коммит:**
   ```powershell
   git commit -m "Initial commit: Auto Paint Shop - Django + React проект"
   ```

4. **Подключите удаленный репозиторий (если еще не подключен):**
   ```powershell
   git remote add origin https://github.com/ВАШ_USERNAME/auto_paint_shop.git
   ```
   Замените `ВАШ_USERNAME` на ваш GitHub username

5. **Отправьте код на GitHub:**
   ```powershell
   git push -u origin main
   ```

## 📝 Детальная инструкция

### Шаг 1: Проверка текущего статуса

```powershell
git status
```

### Шаг 2: Добавление файлов

Добавить все файлы:
```powershell
git add .
```

Или добавить конкретные файлы:
```powershell
git add README.md
git add backend/
git add frontend/
```

### Шаг 3: Создание коммита

```powershell
git commit -m "Описание изменений"
```

**Примеры сообщений коммитов:**
- `"Добавлен желто-черный дизайн интерфейса"`
- `"Улучшена обработка slug для продуктов"`
- `"Добавлены скрипты для автоматического запуска на Windows"`
- `"Исправлены ошибки API и добавлена валидация"`

### Шаг 4: Отправка на GitHub

```powershell
git push origin main
```

Если это первый push:
```powershell
git push -u origin main
```

## 🔧 Полезные команды

### Просмотр изменений
```powershell
git status          # Статус репозитория
git diff            # Показать изменения
git log             # История коммитов
```

### Отмена изменений
```powershell
git restore <файл>  # Отменить изменения в файле
git restore .       # Отменить все изменения
```

### Работа с ветками
```powershell
git branch                    # Список веток
git branch <имя>              # Создать ветку
git checkout <имя>            # Переключиться на ветку
git checkout -b <имя>        # Создать и переключиться
```

## ⚠️ Важные замечания

### Предупреждения о LF/CRLF

Если видите предупреждения типа:
```
warning: LF will be replaced by CRLF
```

Это нормально для Windows. Чтобы убрать предупреждения:
```powershell
git config --global core.safecrlf false
```

Или используйте файл `.gitattributes` (уже создан в проекте) для правильной обработки разных типов файлов.

### Файлы, которые НЕ должны попасть в GitHub:

Убедитесь, что в `.gitignore` есть:
- `venv/` - виртуальное окружение
- `.env` - переменные окружения
- `db.sqlite3` - база данных (опционально)
- `node_modules/` - зависимости Node.js
- `__pycache__/` - кэш Python
- `*.pyc` - скомпилированные файлы Python

### Перед первым push:

1. **Проверьте .gitignore:**
   ```powershell
   cat .gitignore
   ```

2. **Убедитесь, что секретные данные не попали в репозиторий:**
   ```powershell
   git status
   ```

3. **Если нужно, создайте .env.example:**
   ```powershell
   # Создайте файл .env.example с примерами переменных (без реальных значений)
   ```

## 🎯 Быстрая команда для первого push

Если репозиторий уже создан на GitHub:

```powershell
git add .
git commit -m "Initial commit: Auto Paint Shop project"
git push -u origin main
```

## 📚 Дополнительные ресурсы

- [GitHub Docs](https://docs.github.com/)
- [Git Handbook](https://guides.github.com/introduction/git-handbook/)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

