# Руководство по авторизации и регистрации

## Дата: 6 декабря 2025

---

## 🔐 Система авторизации

Проект использует **Token Authentication** для авторизации пользователей.

---

## 📝 Регистрация пользователя

### Frontend: `/register`

**Поля формы:**
- **Username** (обязательно) - минимум 3 символа
- **Email** (обязательно) - валидный email адрес
- **Password** (обязательно) - минимум 8 символов
- **Re-password** (обязательно) - подтверждение пароля
- **Phone** (необязательно) - номер телефона

### API Endpoint

**POST** `/api/accounts/auth/users/`

**Заголовки:**
```
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "username": "john_doe",
  "email": "john@example.com",
  "password": "securepassword123",
  "re_password": "securepassword123",
  "phone": "+375 (33) 123-45-67"
}
```

**Успешный ответ (201 Created):**
```json
{
  "id": 1,
  "username": "john_doe",
  "email": "john@example.com",
  "phone": "+375 (33) 123-45-67"
}
```

**Ошибки:**
- `400 Bad Request` - неверные данные
- Примеры ошибок:
  - "Пользователь с таким именем уже существует"
  - "Пользователь с таким email уже существует"
  - "Пароли не совпадают"
  - "Пароль должен содержать минимум 8 символов"

---

## 🔑 Авторизация (Вход)

### Frontend: `/login`

**Поля формы:**
- **Username** (обязательно)
- **Password** (обязательно)

### API Endpoint

**POST** `/api/accounts/login/`

**Заголовки:**
```
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "username": "john_doe",
  "password": "securepassword123"
}
```

**Успешный ответ (200 OK):**
```json
{
  "auth_token": "9944b09199c62bcf9418ad846dd0e4bbdfc6ee4b",
  "user_id": 1,
  "username": "john_doe",
  "email": "john@example.com",
  "phone": "+375 (33) 123-45-67",
  "is_admin": false
}
```

**Ошибки:**
- `400 Bad Request` - не указаны username или password
- `401 Unauthorized` - неверное имя пользователя или пароль

### Что происходит после входа:

1. Токен сохраняется в `localStorage`:
   ```javascript
   localStorage.setItem('token', data.auth_token);
   ```

2. Данные пользователя сохраняются в `localStorage`:
   ```javascript
   localStorage.setItem('user', JSON.stringify({
     id: data.user_id,
     username: data.username,
     email: data.email,
     is_admin: data.is_admin
   }));
   ```

3. Пользователь перенаправляется на главную страницу
4. Navbar обновляется, показывая имя пользователя

---

## 🚪 Выход из системы

### Frontend: Кнопка "Выйти" в Navbar

### API Endpoint

**POST** `/api/accounts/logout/`

**Заголовки:**
```
Authorization: Token 9944b09199c62bcf9418ad846dd0e4bbdfc6ee4b
```

**Успешный ответ (200 OK):**
```json
{
  "message": "Вы успешно вышли из системы"
}
```

### Что происходит при выходе:

1. Токен удаляется на сервере
2. Токен удаляется из `localStorage`
3. Данные пользователя удаляются из `localStorage`
4. Пользователь перенаправляется на главную страницу
5. Navbar обновляется, показывая кнопки "Войти" и "Регистрация"

---

## 👤 Профиль пользователя

### API Endpoint

**GET** `/api/accounts/profile/`

**Заголовки:**
```
Authorization: Token 9944b09199c62bcf9418ad846dd0e4bbdfc6ee4b
```

**Успешный ответ (200 OK):**
```json
{
  "id": 1,
  "username": "john_doe",
  "email": "john@example.com",
  "phone": "+375 (33) 123-45-67",
  "is_admin": false,
  "created_at": "2025-12-06T10:30:00Z"
}
```

---

## 🔒 Использование токена для защищенных запросов

Все запросы к защищенным endpoints требуют токен авторизации:

```javascript
const token = localStorage.getItem('token');

fetch(`${apiUrl}/cart/cart/`, {
  headers: {
    'Authorization': `Token ${token}`
  }
});
```

**Важно:** Используйте `Token`, а не `Bearer`!

---

## 📊 Модель пользователя

### Поля:

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| username | String | Да | Имя пользователя (уникальное) |
| email | String | Да | Email адрес (уникальный) |
| password | String | Да | Пароль (хешируется) |
| phone | String | Нет | Номер телефона |
| address | Text | Нет | Адрес доставки |
| is_admin | Boolean | Нет | Администратор (по умолчанию false) |
| created_at | DateTime | Нет | Дата регистрации (автоматически) |
| updated_at | DateTime | Нет | Дата обновления (автоматически) |

---

## 🧪 Тестирование

### 1. Регистрация через curl

```bash
curl -X POST http://127.0.0.1:8000/api/accounts/auth/users/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "testpass123",
    "re_password": "testpass123",
    "phone": "+375331234567"
  }'
```

### 2. Вход через curl

```bash
curl -X POST http://127.0.0.1:8000/api/accounts/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "testpass123"
  }'
```

### 3. Получение профиля

```bash
TOKEN="ваш_токен_здесь"

curl -X GET http://127.0.0.1:8000/api/accounts/profile/ \
  -H "Authorization: Token $TOKEN"
```

### 4. Выход

```bash
curl -X POST http://127.0.0.1:8000/api/accounts/logout/ \
  -H "Authorization: Token $TOKEN"
```

---

## 🎨 UI/UX особенности

### Navbar

**Для неавторизованных пользователей:**
- Кнопка "Войти"
- Кнопка "Регистрация"

**Для авторизованных пользователей:**
- Отображается имя пользователя с иконкой
- Кнопка "Выйти"
- Счетчик товаров в корзине

### Формы

**Регистрация:**
- Валидация на клиенте (минимальная длина, совпадение паролей)
- Индикатор загрузки при отправке
- Понятные сообщения об ошибках
- Ссылка на страницу входа

**Вход:**
- Автозаполнение (autocomplete)
- Индикатор загрузки при отправке
- Понятные сообщения об ошибках
- Ссылка на страницу регистрации

---

## ⚠️ Важные замечания

1. **Безопасность паролей:**
   - Минимум 8 символов
   - Пароли хешируются на сервере
   - Никогда не отправляйте пароли в открытом виде

2. **Токены:**
   - Токены хранятся в localStorage
   - Токены не имеют срока действия (пока не выполнен logout)
   - При выходе токен удаляется с сервера

3. **Валидация:**
   - Username должен быть уникальным
   - Email должен быть уникальным и валидным
   - Пароли должны совпадать при регистрации

4. **Обработка ошибок:**
   - Все ошибки отображаются пользователю
   - Ошибки логируются в консоль для отладки

---

## 🔄 Обновление страницы после входа/выхода

После входа или выхода страница автоматически обновляется:

```javascript
window.location.reload();
```

Это необходимо для обновления Navbar и других компонентов, зависящих от состояния авторизации.

---

## 📝 Примеры использования в коде

### Проверка авторизации

```javascript
const isAuthenticated = !!localStorage.getItem('token');

if (!isAuthenticated) {
  alert('Пожалуйста, войдите в систему');
  navigate('/login');
  return;
}
```

### Получение данных пользователя

```javascript
const userData = localStorage.getItem('user');
if (userData) {
  const user = JSON.parse(userData);
  console.log(user.username);
}
```

### Защищенный запрос

```javascript
const token = localStorage.getItem('token');

const response = await fetch(`${apiUrl}/cart/cart/add_item/`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Token ${token}`
  },
  body: JSON.stringify({
    product_slug: 'some-product',
    quantity: 1
  })
});
```

---

## ✅ Готово!

Система авторизации и регистрации полностью настроена и готова к использованию.
