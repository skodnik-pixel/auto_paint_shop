# Инструкция по запуску проекта в Windows PowerShell

## 🔧 Настройка PowerShell

Если при запуске скриптов появляется ошибка о политике выполнения, выполните:

```powershell
# Проверьте текущую политику
Get-ExecutionPolicy

# Если политика Restricted, измените её (требуются права администратора):
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Или для одного сеанса (без прав администратора):
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
```

## 🚀 Запуск проекта

### Вариант 1: Запуск всего проекта одной командой (рекомендуется)

Из корневой директории проекта:
```powershell
.\start-dev.ps1
```

Этот скрипт автоматически:
- ✅ Запустит Django сервер (Backend) на `http://127.0.0.1:8000/`
- ✅ Запустит React сервер (Frontend) на `http://localhost:3000`
- ✅ Откроет оба в браузере
- ✅ Покажет логи обоих серверов

**Для остановки:** Нажмите `Ctrl+C`

### Вариант 2: Запуск только Backend

```powershell
cd backend
.\runserver.ps1
```

Или:
```powershell
cd backend
python runserver.py
```

### Вариант 3: Запуск Backend и Frontend отдельно

**Терминал 1 (Backend):**
```powershell
cd backend
python manage.py runserver
```

**Терминал 2 (Frontend):**
```powershell
cd frontend
npm start
```

## 📝 Структура скриптов

- `start-dev.ps1` - запускает весь проект (Backend + Frontend)
- `backend/runserver.ps1` - запускает только Django сервер с автозапуском браузера
- `backend/runserver.py` - Python скрипт для кроссплатформенного запуска
- `backend/runserver.bat` - batch файл для Windows CMD

## ⚠️ Важные замечания

1. **Виртуальное окружение:** Убедитесь, что виртуальное окружение активировано перед запуском:
   ```powershell
   cd backend
   .\venv\Scripts\Activate.ps1
   ```

2. **Порты:** Убедитесь, что порты 8000 (Django) и 3000 (React) свободны.

3. **Зависимости:** Убедитесь, что все зависимости установлены:
   ```powershell
   # Backend
   cd backend
   pip install -r requirements.txt
   
   # Frontend
   cd frontend
   npm install
   ```

## 🐛 Решение проблем

### Ошибка "не удается загрузить файл"
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Порт уже занят
Измените порт в команде:
```powershell
python manage.py runserver 8001
```

### Браузер не открывается при двойном клике на ссылку в терминале

**Это нормальное поведение!** PowerShell не открывает ссылки автоматически при клике.

**Решения:**
1. Используйте скрипты (они открывают браузер автоматически):
   ```powershell
   .\start-dev.ps1
   # или
   cd backend
   .\runserver.ps1
   ```

2. Откройте браузер вручную командой:
   ```powershell
   Start-Process "http://127.0.0.1:8000/"
   ```

3. Скопируйте ссылку и вставьте в браузер:
   - Выделите ссылку в терминале
   - Нажмите `Ctrl+C`
   - Откройте браузер и вставьте в адресную строку

См. подробнее в [QUICK_START.md](QUICK_START.md)

