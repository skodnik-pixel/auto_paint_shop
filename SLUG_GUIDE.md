# Руководство по полю Slug

## Что такое Slug?

**Slug** — это URL-дружественная версия названия товара, категории или бренда. Он используется в URL для доступа к конкретному объекту.

## Формат Slug

Slug должен содержать:
- ✅ **Строчные латинские буквы** (a-z)
- ✅ **Цифры** (0-9)
- ✅ **Дефисы** (-) вместо пробелов
- ❌ **Без пробелов**
- ❌ **Без специальных символов** (!@#$%^&*() и т.д.)
- ❌ **Без заглавных букв**

## Примеры преобразования

| Название | Slug |
|----------|------|
| Краска для автомобиля | `kraska-dlya-avtomobilya` |
| Шампунь для мойки | `shampun-dlya-mojki` |
| 3M Sanding Discs | `3m-sanding-discs` |
| Автокосметика и автохимия | `avtokosmetika-i-avtohimiya` |
| Koch Chemie | `koch-chemie` |

## Как заполнять Slug?

### Вариант 1: Автоматическая генерация (рекомендуется)

В форме создания продукта slug **автоматически генерируется** из названия при вводе. Просто введите название, и slug создастся сам!

**Пример:**
1. Вводите название: `Краска для автомобиля`
2. Slug автоматически станет: `kraska-dlya-avtomobilya`

### Вариант 2: Ручной ввод

Если нужно изменить slug вручную:
1. Введите название товара
2. Отредактируйте поле Slug по необходимости
3. Убедитесь, что формат правильный (только латиница, цифры, дефисы)

## Правила создания Slug

1. **Транслитерация кириллицы:**
   - Кириллические буквы автоматически преобразуются в латиницу
   - `а` → `a`, `б` → `b`, `в` → `v` и т.д.

2. **Удаление специальных символов:**
   - Все символы кроме букв, цифр и пробелов удаляются
   - `Краска №1!` → `kraska-1`

3. **Замена пробелов на дефисы:**
   - Пробелы заменяются на дефисы
   - `Краска для авто` → `kraska-dlya-avto`

4. **Удаление множественных дефисов:**
   - Несколько дефисов подряд заменяются одним
   - `краска---для---авто` → `kraska-dlya-avto`

5. **Удаление дефисов в начале и конце:**
   - Дефисы в начале и конце удаляются
   - `-kraska-dlya-avto-` → `kraska-dlya-avto`

## Важные замечания

⚠️ **Slug должен быть уникальным!** Если товар с таким slug уже существует, вы получите ошибку.

💡 **Совет:** Если slug уже занят, добавьте в конец номер или дополнительное слово:
- `kraska-dlya-avtomobilya` (занят)
- `kraska-dlya-avtomobilya-2` (свободен)
- `kraska-dlya-avtomobilya-premium` (свободен)

## Использование Slug в URL

Slug используется для создания красивых URL:

```
/product/kraska-dlya-avtomobilya/  ✅ Хорошо
/product/123/                       ❌ Плохо (использует ID)
```

## Проверка Slug

Перед сохранением убедитесь, что slug:
- ✅ Содержит только латинские буквы (строчные), цифры и дефисы
- ✅ Не начинается и не заканчивается дефисом
- ✅ Не содержит пробелов
- ✅ Уникален (не используется другим товаром)

## Автоматическая генерация в Django Admin

В Django Admin панели slug автоматически генерируется из названия благодаря настройке `prepopulated_fields`. Это работает только в админке, не в API.

